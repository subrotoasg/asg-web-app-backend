export const ACTIVE_WINDOW_MS = 90 * 1000;

export const CLASS_KEY_EXPIRE_SEC = 60 * 60;

export const COUNT_EMIT_THROTTLE_MS = 5000;

export const MIN_HEARTBEAT_INTERVAL_MS = 10000;

export const VALID_CLASS_TYPES = new Set([
  "CLASS_CONTENT",
  "CYCLE_CONTENT",
  "LIVE_CLASS",
]);
